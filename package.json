{
  "private": true,
  "description": "Monorepo root - dependencies should be in individual projects (backend-api, frontend-web, mobile-app)",
  "scripts": {
    "backend": "cd backend-api && npm run start:dev",
    "frontend": "cd frontend-web && npm run dev",
    "mobile": "cd mobile-app && npm start"
  }
}
