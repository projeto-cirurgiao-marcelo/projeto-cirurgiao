# Web production smoke — 2026-07-02

Smoke do canal **web** (Vercel canônica). Ver headers via `curl` (automatizável)
+ checklist funcional manual no navegador.

## URL testada

| Campo | Valor |
|---|---|
| **URL** | `https://projeto-cirurgiao.vercel.app` |
| Origem | Domínio default do projeto Vercel (não estava explícito no repo; inferido do slug + confirmado ao vivo). |
| Confirmação | `X-Vercel-Id: gru1::...` (região `gru1` bate com `vercel.json`) + CSP idêntica à do commit `635d694` → é o deploy Vercel **deste** projeto. |
| ⏳ Pendente | Gustavo confirmar se esta é a URL **canônica** de produção ou se o alvo é o domínio custom `app.projetocirurgiao.app` (não resolvia do ambiente do agente). |

## 1. Smoke de headers (HTTP) — ✅ EXECUTADO (2026-07-02)

```bash
curl -I https://projeto-cirurgiao.vercel.app
```

| Header esperado (P1.15) | Presente? | Valor observado |
|---|---|---|
| `content-security-policy` | ✅ | `default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob: https:; font-src 'self' data: https://fonts.gstatic.com; media-src 'self' blob: https:; connect-src 'self' https: wss:; frame-src 'self' https://iframe.videodelivery.net https://*.cloudflarestream.com https://www.youtube.com https://player.vimeo.com; worker-src 'self' blob:; object-src 'none'; base-uri 'self'; frame-ancestors 'self'` |
| `x-content-type-options` | ✅ | `nosniff` |
| `referrer-policy` | ✅ | `strict-origin-when-cross-origin` |
| `x-frame-options` | ✅ | `SAMEORIGIN` |
| `permissions-policy` | ✅ | `camera=(), microphone=(), geolocation=()` |
| `strict-transport-security` (bônus, Vercel) | ✅ | `max-age=63072000; includeSubDomains; preload` |

**Resultado:** `HTTP/1.1 200 OK`. **Todos os 5 headers do P1.15 presentes e corretos em produção.**
CSP ainda com `'unsafe-inline'`/`'unsafe-eval'` por compatibilidade (débito conhecido — apertar com nonces em rodada futura, ver `frontend-web/docs/DEPLOY.md §2.1`).

## 2. Smoke funcional (navegador) — ⏳ PENDENTE (execução manual)

Requer navegador + conta de teste (e conta admin para os 2 últimos itens). O agente
é headless; **executar manualmente** e preencher a tabela de achados abaixo.

- [ ] Login (conta de teste)
- [ ] Dashboard aluno carrega (sem tela branca)
- [ ] Player HLS (play + seek)
- [ ] Legenda pt-BR (aparece/alterna)
- [ ] Quiz (responder + resultado)
- [ ] Fórum (listar, abrir tópico)
- [ ] Chat / RAG (perguntar + resposta)
- [ ] Logout (limpa sessão)
- [ ] Admin dashboard (se houver conta admin)
- [ ] R2 browser / admin media (se houver conta admin)

## 3. Tabela de achados

Preencher na execução funcional. Severidade: bloqueante / alto / médio / baixo.

| Fluxo | Status | Evidência | Severidade | Ação recomendada |
|---|---|---|---|---|
| Headers de segurança (curl) | ✅ OK | seção 1 (todos os 5 + HSTS presentes) | — | nenhuma (débito CSP nonce = futuro) |
| Login | ⏳ | | | |
| Dashboard aluno | ⏳ | | | |
| Player HLS | ⏳ | | | |
| Legenda pt-BR | ⏳ | | | |
| Quiz | ⏳ | | | |
| Fórum | ⏳ | | | |
| Chat / RAG | ⏳ | | | |
| Logout | ⏳ | | | |
| Admin dashboard | ⏳ | | | |
| R2 browser / admin media | ⏳ | | | |

---

**Status geral:** headers ✅ validados em prod; funcional ⏳ aguardando execução manual.
Bug encontrado no funcional → abrir **tarefa separada** (não corrigir dentro do smoke).
