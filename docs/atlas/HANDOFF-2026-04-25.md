# Atlas Migration — Session Handoff

**Data:** 2026-04-25
**Worktree:** `D:\dashboard\next-shadcn-admin-dashboard-main\.worktrees\video-player-skin\`
**Frontend dev port:** 3002
**Branch:** main (no worktree video-player-skin)
**Modelo de DS:** `docs/atlas/DESIGN_SYSTEM.md` (v2 + §8 Feedback patterns)
**Refs canônicas:** `docs/atlas/reference-{desktop,listing-pages,mobile}.html`
**Audit:** `docs/atlas/sprint-6-audit.md`

---

## TL;DR

Sprints 1–6 + 6.5 + extras concluídos. **`/student/**` ~93% Atlas (13/14 rotas)**. Falta gamification, quiz, polimento de itens baixa do audit, e Atlas Admin (Sprint 7+).

`tsc --noEmit -p tsconfig.json` exit 0 em todas as iterações.

---

## 1. Sprints concluídos

### Sprint 1 — Fundação ✅
- Tokens semânticos `--atlas-*` em `globals-premium.css` (light + dark)
- Source Serif 4 importada via Google Fonts
- 16 utility classes `bg-atlas-*` / `text-atlas-*` via `@theme inline`
- `.atlas-caps`, `.atlas-mono`, `.atlas-num`, `.atlas-key` em `@layer utilities`
- Primitives: `AtlasButton`, `AtlasCard*` em `src/components/atlas/primitives/`
- Smoke test: rota `/atlas-smoke` (válida, segue lá)

### Sprint 2 — Catalog (`/student/courses` Template A) ✅
- `AtlasPageHeader`, `AtlasStatsInline`, `AtlasFiltersRow`, `AtlasEmptyState`, `AtlasLoadingBar`, `AtlasSkeletonCard`, `AtlasCompletionStrip`
- `AtlasSectionTabs`
- `AtlasCourseCard`, `AtlasCourseThumb`
- `/student/courses` refatorado pra Template A

### Sprint 3 — Listings B/C/D ✅
- `/student/in-progress` Template B (lista densa via `AtlasCourseRow`)
- `/student/completed` Template C
- `/student/my-courses` Template D (3 seções verticais sem carrossel — DS rejeita Netflix)
- Bug fix: thumbnails do banco (`thumbnailHorizontal/Vertical/thumbnail` fallback chain)
- Bug fix: stat "Taxa de conclusão" tautológica → dinâmica `completed / enrolled`

### Sprint 4 — Watch page envelope ✅
- `AtlasLessonHeader`, `AtlasLessonInfo`, `AtlasLessonStats`, `AtlasPlayerWrapper`, `AtlasStickyActions`
- `AtlasModuleSidebar` (flush right desktop) + `AtlasStagesProgress`
- Refactor `/student/courses/[id]/watch/[videoId]/page.tsx` (visual envelope only — lógica preservada: HlsVideoPlayer, captions, autosave, sendBeacon, bookmarks, share, etc.)

### Sprint 4.5 — Re-skin componentes legados (8/8) ✅
| # | Componente | Atlas primitivos novos |
|---|---|---|
| 1 | VideoSummaries | synthesis/{Card,Skeleton,Empty} |
| 2 | QuizCard | course/AtlasAssessmentCTA |
| 3 | VideoNotes + Compact | notes/{Row,Editor,List} |
| 4 | VideoMaterialsCarousel + CompactList | materials/{Row,List} |
| 5 | VideoBookmarksPanel | bookmarks/{Row,List} |
| 6 | VideoChaptersPanel | chapters/{Row,List} |
| 7 | VideoLikeButton + Badge | (usa AtlasButton) |

### Sprint 5 — Shell (Rail + TopBar + AdminStrip) ✅
- `AtlasRail` (collapse via `useSidebarStore`, sections + items + footer slot)
- `AtlasRailUser` (avatar + nome + role + chevron)
- `AtlasTopBar` + `AtlasIconButton` (breadcrumb dinâmico + search + trailing)
- `AtlasAdminStrip` (bg-ink, "Voltar ao painel" + dot warn)
- `student-sidebar.tsx`, `student-header.tsx`, `admin-sidebar.tsx`, `admin-header.tsx` reescritos como wrappers Atlas (mantêm exports originais)
- Layout (`(dashboard)/student/layout.tsx`) inalterado

### Sprint 6 — Mobile responsive sweep ✅
- **Audit doc:** `docs/atlas/sprint-6-audit.md` (34 itens mapeados em 5 viewports 320/375/414/768/1024)
- **AtlasTabBar global** built + integrated no layout student (5 slots: Cursos / Em curso / Atlas / Fórum / Perfil)
- **Dia 1 watch mobile:** AtlasLessonInfo stack, AtlasLessonHeader stack + dual mode (compact mobile / full desktop), `AtlasModuleSheet` (Radix Dialog bottom sheet + safe-area), Player controls media-chrome `@media (max-width: 639px)` overrides, FAB chat reskin (chat-widget + video-chat-widget — purple/emerald gradient → atlas-surface border + primary text), AtlasStickyActions safe-area, AdminStrip label hide, T3 `touch-action: manipulation` global em buttons
- **Dia 2 listings:** AtlasPageHeader stack vertical, AtlasStatsInline grid 2×2 mobile, AtlasFiltersRow chip overflow scroll-snap, AtlasSectionTabs overflow-x scroll, AtlasCourseRow → mobile card stack (thumb top + content + progress + CTA full-width)
- **Dia 3 médios:** SynthesisCard footer wrap, AssessmentCTA grid narrow, CompletionStrip wrap, EmptyState padding, NoteRow grid `[44px_1fr_auto]` mobile, NoteEditor counter wrap, MaterialRow meta hide narrow, ChapterRow line-clamp, AtlasButton tap targets `h-9 sm:h-8` (era h-8 fixo violando 44px), actions notes/bookmarks visíveis em mobile (`sm:opacity-0 sm:group-hover:opacity-100`)
- **Burger button hidden em watch route mobile** (regex pathname check)
- **Topbar global hidden em watch route mobile** + layout `mt-0 md:mt-16` adaptativo
- **Watch mobile redesign aprovado:** AtlasLessonHeader compact `← Voltar` + `01/05` mono + barra fina 2px (oculta caps + h1 grande em mobile), AtlasLessonInfo com `inlineAction` (Like inline com title) + `contextLine` (mono "aula 05/05"), botões Anterior/Marcar/Próxima `hidden sm:flex` (substituídos pela bottom bar)

### Sprint 6.5 — Course Detail ✅
- `/student/courses/[id]` refatorado: AtlasPageHeader + hero banner aspect 16/7 sm:21/7 lg:6/1 com thumbnail + AtlasStatsInline + AtlasStagesProgress + grid `AtlasModuleCard` (novo)
- `/student/courses/[id]/modules/[moduleId]` refatorado: hero banner do módulo + AtlasPageHeader + AtlasStatsInline + lista de aulas com status icons (Check/Clock/Play/Lock) e disabled state pra `!isPublished`

### Extras pós-Sprint 6
- **Toast hierarchy + AtlasToast wrapper** (Frente 2): `src/components/atlas/feedback/AtlasToast.tsx` com 4 níveis documentados em DS §8. Sonner CSS reskin: surface + border-left colorida (não saturated full-bleed). 7 toasts removidos (nível 0 — UI já confirma), 8 toasts atualizados.
- **DS §8 Feedback patterns** documentado, seções 8-12 renumeradas → 9-13
- **`/student/library`** Atlas reskin (chat IA Veterinária)
- **`/student/profile`** Atlas reskin completo (avatar + form perfil + form senha)
- **`/student/forum/**`** 4 rotas + CategoryCard + TopicCard reskinados (emojis removidos, Lucide icons por keyword)
- **Seed script forum categories** `backend-api/scripts/seed-forum-categories.ts` — 11 categorias idempotentes

---

## 2. Componentes Atlas inventário (40+)

```
src/components/atlas/
├── primitives/
│   ├── AtlasButton.tsx           (variants primary/outline/ghost × sm/md/lg/icon/icon-sm)
│   └── AtlasCard.tsx             (Card + Header/Title/Content/Footer)
├── page/
│   ├── AtlasPageHeader.tsx
│   ├── AtlasStatsInline.tsx      (grid 2×2 mobile, flex linear desktop)
│   ├── AtlasFiltersRow.tsx       (search + chips com scroll-snap mobile)
│   ├── AtlasEmptyState.tsx
│   ├── AtlasLoadingBar.tsx
│   ├── AtlasSkeletonCard.tsx
│   └── AtlasCompletionStrip.tsx  (border-l-3 success/info)
├── navigation/
│   └── AtlasSectionTabs.tsx       (overflow scroll mobile)
├── course/
│   ├── AtlasCourseCard.tsx
│   ├── AtlasCourseRow.tsx         (mobile card stack / desktop grid 4-col)
│   ├── AtlasCourseThumb.tsx       (5 variants gradient)
│   ├── AtlasModuleCard.tsx        (curso detail)
│   ├── AtlasModuleSidebar.tsx     (flush + hideHeader props)
│   ├── AtlasModuleSheet.tsx       (Radix Dialog bottom sheet)
│   ├── AtlasStagesProgress.tsx
│   └── AtlasAssessmentCTA.tsx
├── lesson/
│   ├── AtlasLessonHeader.tsx      (dual mode mobile compact / desktop full)
│   ├── AtlasLessonInfo.tsx        (inlineAction + contextLine)
│   ├── AtlasLessonStats.tsx
│   ├── AtlasPlayerWrapper.tsx
│   └── AtlasStickyActions.tsx     (safe-area)
├── notes/
│   ├── AtlasNoteRow.tsx           (grid adaptativo mobile)
│   ├── AtlasNoteEditor.tsx
│   └── AtlasNotesList.tsx
├── materials/
│   ├── AtlasMaterialRow.tsx
│   └── AtlasMaterialsList.tsx
├── bookmarks/
│   ├── AtlasBookmarkRow.tsx
│   └── AtlasBookmarksList.tsx
├── chapters/
│   ├── AtlasChapterRow.tsx
│   └── AtlasChaptersList.tsx
├── synthesis/
│   ├── AtlasSynthesisCard.tsx
│   ├── AtlasSynthesisCardEmpty.tsx
│   └── AtlasSynthesisCardSkeleton.tsx
├── shell/
│   ├── AtlasRail.tsx              (pull-tab toggle, collapsed 80 / expanded 240)
│   ├── AtlasRailUser.tsx
│   ├── AtlasTopBar.tsx + AtlasIconButton (search center mobile, ml-auto desktop)
│   ├── AtlasAdminStrip.tsx
│   └── AtlasTabBar.tsx            (5 slots fixed bottom mobile + safe-area)
├── feedback/
│   └── AtlasToast.tsx             (atlasToast.* + AtlasToaster)
└── index.ts                       (re-exports)
```

---

## 3. Rotas `/student/**` — status Atlas

| Rota | Status |
|---|---|
| `/student/courses` | ✅ Template A |
| `/student/in-progress` | ✅ Template B |
| `/student/completed` | ✅ Template C |
| `/student/my-courses` | ✅ Template D |
| `/student/courses/[id]` | ✅ Course detail + hero thumbnail |
| `/student/courses/[id]/modules/[moduleId]` | ✅ Module detail + hero thumbnail |
| `/student/courses/[id]/watch/[videoId]` | ✅ Watch + mobile compact + sheet |
| `/student/library` | ✅ Chat IA |
| `/student/profile` | ✅ Form perfil/senha |
| `/student/forum` | ✅ Categorias |
| `/student/forum/[categoryId]` | ✅ Lista tópicos |
| `/student/forum/[categoryId]/new` | ✅ Form criar tópico |
| `/student/forum/topic/[topicId]` | ✅ Visualizar + responder |
| `/student/gamification` | ❌ **deferido** |
| `/student/courses/[id]/quiz/[quizId]` | ❌ **deferido** |

13/15 rotas Atlas. Faltam 2.

---

## 4. Backend (não tocado, exceto seed)

- 1 script novo: `backend-api/scripts/seed-forum-categories.ts` — 11 categorias canônicas idempotentes
- Schema Prisma `ForumCategory` já tinha campos `icon` (Lucide name) e `color` (hex) — populados pelo seed mas ainda não consumidos pelo frontend
- Comando: `cd backend-api && npx tsx scripts/seed-forum-categories.ts`
- Roda quantas vezes precisar (idempotente via name lookup case-insensitive)

---

## 5. Decisões importantes (referência)

- **Carrossel Netflix-style proibido na área Atlas** (DS §1) — `/my-courses` foi reescrito como agregação vertical
- **Emoji em label proibido** (DS §1) — CategoryCard usa Lucide icons por keyword
- **Stats tautológicas removidas** — taxa de conclusão dinâmica
- **Toast hierarchy 4 níveis** documentada em DS §8: 0=silencioso (UI confirma), 1=minimal 2.5s, 2=undo 5s, 3=erro persistente
- **`atlasToast.*` é canal único** — não usar `toast` do sonner direto em código novo
- **Tap targets** — AtlasButton size adaptativo `h-9 sm:h-8` (mobile prioriza acessibilidade)
- **Mobile nav** = AtlasTabBar bottom (não drawer hamburger) — drawer mantido como fallback para rotas fora dos 5 slots
- **Watch route mobile** — topbar global + burger button hidden, AtlasLessonHeader compact assume header
- **MobileSidebar drawer** mantida (cobre rotas fora do tab bar: my-courses, completed, gamification)
- **VideoChatWidget + ChatWidget global** — ambos FABs movidos pra direita; em watch mobile ficam acima da bottom bar via `bottom-[calc(72px+env(safe-area-inset-bottom))]`

---

## 6. Bugs identificados pendentes

Nenhum bloqueante conhecido. tsc clean em todas iterações.

Riscos mapeados pra acompanhar:
- AtlasLessonHeader em mobile redesign mostra apenas progressDone/Total — está OK porque o lesson info abaixo do player tem contextLine "aula NN/NN" redundante (deliberado, não bug)
- Burger button hidden em watch mobile = aluno sem drawer rápido pra outras rotas. Mitigação: "← Voltar" leva pra `/student/my-courses` onde drawer aparece
- VideoChatWidget na watch route + ChatWidget global = empilhamento direita. Mitigação: video-chat-widget só renderiza em watch route, então só uma FAB ativa por vez por rota — verificar visual quando logged
- Hero banner course detail / module detail só renderiza com thumbnail no banco. Sem thumbnail = cole AtlasPageHeader direto no topo (comportamento esperado)

---

## 7. Pendências pra próxima sessão

### Curtas (≤ 1h cada)
1. **Forum categories seed run + validar** — rodar `npx tsx scripts/seed-forum-categories.ts` no backend e testar fluxo completo (criar tópico, responder, votar, denunciar)
2. **`/student/gamification`** Atlas reskin (não auditado, escopo desconhecido — provavelmente badges + xp + leaderboard)
3. **`/student/courses/[id]/quiz/[quizId]`** Atlas reskin (form de quiz com questões)
4. **Itens baixa do audit (30-33)** — refinamento opcional

### Médias (1-3h)
5. **Sprint 6 Dia 4 buffer:** smoke visual real em DevTools mobile 320/375/414/768/1024 pra cada rota student
6. **AtlasModuleSheet validation** — testar mobile sheet abrir/fechar/swipe-down com teclado, scroll lock, tap fora
7. **`/admin/**` topbar still has `toast.*` direto** — migrar pra `atlasToast` (audit em admin/videos, admin/modules, admin/settings, admin/captions)

### Longas (sprint dedicado)
8. **Sprint 7 — Atlas Admin Profile DS spec** — DS adendo §14 com componentes admin (DataTable, FormPanel, ConfirmDialog, ToolbarBulkActions, FileUpload)
9. **Sprints 8-9** — implementar componentes admin + migrar telas
10. **Re-skin de admin layouts** (admin-sidebar, admin-header já são wrappers Atlas, mas conteúdo das pages admin é shadcn legado)

---

## 8. Comandos úteis

```bash
# Worktree path
cd D:/dashboard/next-shadcn-admin-dashboard-main/.worktrees/video-player-skin

# Frontend dev (já rodando na 3002)
cd frontend-web && PORT=3002 npx next dev -p 3002

# Type-check (gate antes de commit)
cd frontend-web && npx tsc --noEmit -p tsconfig.json

# Smoke test rota standalone Atlas
curl -s -o /dev/null -w "atlas-smoke %{http_code}\n" http://localhost:3002/atlas-smoke

# Backend Prisma (Cloud SQL ou docker-compose local)
cd backend-api && docker-compose up -d

# Seed forum categories
cd backend-api && npx tsx scripts/seed-forum-categories.ts

# Validar AtlasToast funcional
# 1. Abre /student/forum/[categoryId]/new
# 2. Submit sem preencher → atlasToast.warning surge top-right surface + border-l warn
# 3. Erro de rede → atlasToast.error persistente

# Validar mobile redesign
# DevTools → Device Mode → iPhone 12 (390×844)
# /student/courses/<id>/watch/<videoId>
# Esperado: topbar global some, burger some, mini-bar 40px topo aparece (← Voltar | 01/05 + barra fina)
```

---

## 9. Arquivos chave alterados/criados nesta sessão

### Criados (40+ Atlas components + 1 seed)
- `src/components/atlas/**` (todos os 40+ componentes)
- `src/components/atlas/feedback/AtlasToast.tsx`
- `src/components/atlas/index.ts` (re-exports)
- `src/app/atlas-smoke/page.tsx` (smoke route)
- `docs/atlas/sprint-6-audit.md`
- `docs/atlas/HANDOFF-2026-04-25.md` (este doc)
- `backend-api/scripts/seed-forum-categories.ts`

### Modificados (rotas refatoradas)
- `src/app/globals-premium.css` (tokens + utilities + media queries player + sonner reskin + scrollbar-hide + touch-action)
- `src/app/layout.tsx` (Toaster → AtlasToaster)
- `src/app/(dashboard)/student/layout.tsx` (AtlasTabBar + pathname check)
- `src/app/(dashboard)/student/{courses,in-progress,completed,my-courses,library,profile,forum,**}/page.tsx`
- `src/app/(dashboard)/student/courses/[id]/{page,watch/[videoId]/page,modules/[moduleId]/page}.tsx`
- `src/components/layout/{student,admin}-{sidebar,header}.tsx`
- `src/components/forum/{category-card,topic-card}.tsx`
- `src/components/chatbot/{chat-widget,video-chat-widget}.tsx`
- `src/components/video-player/{video-summaries,video-notes,video-materials-carousel,video-bookmarks-panel,video-chapters-panel,video-like-button,share-to-forum-dialog}.tsx`
- `src/components/quiz/quiz-card.tsx`
- `src/lib/api/progress.service.ts` (extended `EnrolledCourseWithProgress` interface com `thumbnailHorizontal/Vertical`)
- `docs/atlas/DESIGN_SYSTEM.md` (§8 Feedback patterns inserido, §9-13 renumerados)

---

## 10. Critérios de aceite acumulados

✅ tsc exit 0 em todas iterações
✅ /atlas-smoke 200 OK + CSS compilado tem 20 classes Atlas + Source Serif 4
✅ Dev server 3002 funcional
✅ Listing pages (4 templates) Atlas + mobile responsive
✅ Watch page envelope Atlas + mobile redesign aprovado
✅ 8 componentes legados internos re-skinned
✅ Shell (rail/topbar/admin strip) Atlas
✅ Toast hierarchy + atlasToast canal único
✅ Course/module detail pages Atlas
✅ Library + Profile Atlas
✅ Forum 4 rotas + 2 cards Atlas
✅ Seed script forum categories pronto
✅ Audit doc com 34 itens classificados
✅ HANDOFF doc (este)

---

## 11. Onde retomar amanhã

**Opção A — Continuar /student/**:**
1. Rodar seed forum + testar fluxo completo
2. `/student/gamification` reskin
3. `/student/courses/[id]/quiz/[quizId]` reskin

**Opção B — Sprint 7 Atlas Admin:**
1. DS §14 spec admin (DataTable, FormPanel, etc)
2. Migrar admin/videos, admin/modules, admin/settings

**Opção C — Polimento Sprint 6:**
1. Smoke visual real em viewport com DevTools
2. Dia 4 buffer (itens 30-33 baixa)
3. AtlasModuleSheet validation iOS Safari real

Recomendação: Opção A primeiro (fechar /student/ 100%), depois B.
